#include <iostream>
#include <fstream>
#include <cstdlib>
using namespace std;
int main()
{   
    int S-id;
    int a=0;
    fstream file;
    file.open(("StudentDataBase.dat",ios::in|ios::out));
    srand (time(NULL));
    a = rand() % 10 + 22;
    cout << a;

    for(int i = 0; i <100;i++)
    return 0;
}