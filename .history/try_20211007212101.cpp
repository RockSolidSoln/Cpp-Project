#include <iostream>
#include <fstream>
#include <cstdlib>
using namespace std;
int main()
{   
    int Sid=1191300001;
    int a=0,m1=0,m2=0,m3=0;
    ofstream file;
    file.open(("StudentDataBase.dat",ios::in|ios::out));
    srand (time(NULL));

    for(int i = 0; i <100;i++)
    {   if (myfile.is_open()
        {
            
        }
    }
    file.close();
    return 0;
}